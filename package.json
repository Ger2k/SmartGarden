{
  "name": "smart-garden",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest --exclude tests/rules/**",
    "test:run": "vitest run --exclude tests/rules/**",
    "test:rules:run": "vitest run tests/rules/firestore.rules.test.ts",
    "test:rules": "firebase emulators:exec --project demo-smart-garden --only firestore \"npm run test:rules:run\"",
    "clean:vite": "node -e \"const fs=require('fs');fs.rmSync('node_modules/.vite',{recursive:true,force:true});console.log('node_modules/.vite cleaned');\"",
    "verify": "npm run test:run && npm run build",
    "verify:full": "npm run verify && npm run test:rules"
  },
  "dependencies": {
    "@astrojs/netlify": "^7.0.6",
    "@astrojs/react": "^5.0.3",
    "@tailwindcss/vite": "^4.2.2",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "astro": "^6.1.6",
    "dayjs": "^1.11.20",
    "firebase": "^12.12.0",
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "tailwindcss": "^4.2.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "@vitest/coverage-v8": "^4.1.4",
    "firebase-tools": "^15.14.0",
    "vitest": "^4.1.4"
  }
}
